export const metadata = {
  title: "Shopping Cart",
  description: "Review your Budy Bear cart, update quantities, and continue to checkout.",
};

export default function CartLayout({ children }) {
  return children;
}
