export const metadata = {
  title: "Checkout",
  description: "Complete your Budy Bear order with Cash on Delivery or bank transfer.",
};

export default function CheckoutLayout({ children }) {
  return children;
}
